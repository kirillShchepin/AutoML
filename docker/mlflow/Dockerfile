FROM python:3.11-slim

WORKDIR /mlflow

RUN pip install --no-cache-dir mlflow==2.9.2 boto3==1.28.76 psycopg2-binary==2.9.9

EXPOSE 5000
CMD ["bash", "-c", "mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri $MLFLOW_BACKEND_STORE_URI --default-artifact-root $MLFLOW_ARTIFACT_ROOT"]
